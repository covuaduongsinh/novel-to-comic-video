# Framework web (flask>=3 đi kèm werkzeug>=3, tránh lỗi import url_quote)
flask>=3.0.0
flask-cors>=4.0.0

# Biến môi trường
python-dotenv==1.0.0

# Xử lý hình ảnh (Pillow<10 tương thích moviepy 1.0.3; numpy<2 tương thích ABI cũ)
Pillow<10
numpy<2
# transformers 5.x sẽ khởi tạo sai CLIP text encoder của SD v1.5 (512 vs 768),
# bắt buộc cố định ở 4.x, kèm theo diffusers/huggingface_hub phiên bản cũ.
transformers==4.46.3
diffusers==0.31.0
huggingface_hub==0.26.5
tokenizers==0.20.3
accelerate>=0.26.0
torch>=2.0.0
torchvision>=0.15.0

# Xử lý video
moviepy==1.0.3
ffmpeg-python==0.2.0

# Xử lý âm thanh
pydub==0.25.1

# Xử lý văn bản
jieba>=0.42.1
nltk>=3.8.1

# Thư viện tiện ích
tqdm>=4.65.0
requests>=2.28.2
